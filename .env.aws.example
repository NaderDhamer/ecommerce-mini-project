# Copy to .env on EC2 (or use terraform user_data /opt/ecommerce/.env)
DB_PASSWORD=ChangeMeInLearnerLab123!
# Set after you know the public IP (deploy script sets this automatically)
# CORS_ORIGINS=http://3.85.0.0,http://localhost

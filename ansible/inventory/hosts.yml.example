# Copy to hosts.yml and set your EC2 public IP + key path (hosts.yml is gitignored).
all:
  hosts:
    ecommerce:
      ansible_host: 44.223.34.77
      ansible_user: ec2-user
      ansible_ssh_private_key_file: ~/.ssh/your-learner-lab-key.pem
